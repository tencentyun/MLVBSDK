<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- 高度40 -->
    <size
        android:width="20dp"
        android:height="20dp" />
    <!-- 圆角弧度 20 -->
    <corners android:radius="10dp" />

    <!-- 变化率 -->
    <gradient
        android:endColor="@color/livepusher_gray_white"
        android:startColor="@color/livepusher_gray_white" />

    <stroke
        android:width="1dp"
        android:color="@color/livepusher_gray" />

</shape>
